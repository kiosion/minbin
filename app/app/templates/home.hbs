{{page-title "Home"}}

<RouteWrapper>
  <Actions::Box
    @mode="home"
    @fns={{hash
      create=(fn this.transitionTo "new")
    }}
  />
  <Paste::Form
    @mode="show"
    @plaintext={{true}}
    @paste={{this.paste}}
  />
</RouteWrapper>
